import fixed from './fixed';
import current from './current';
import fund from './fund';

const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  fixed,
  current,
  fund,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
