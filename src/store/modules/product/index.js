import fixed from './fixed';
import current from './current';

const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  fixed,
  current,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
