import fixed from './fixed';

const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  fixed,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
