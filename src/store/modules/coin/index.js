import purchaseCoin from './purchaseCoin';


const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  purchaseCoin,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
