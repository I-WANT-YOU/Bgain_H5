import userAssets from './userAssets';

const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  userAssets,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
